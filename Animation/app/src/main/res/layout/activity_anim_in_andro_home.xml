<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/lay1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/background_light"
    android:visibility="visible"
    tools:context="com.example.angry.animation.AnimInAndroHome">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:text="ANIMATION IN ANDROID"
        android:textColor="@android:color/black"
        android:textSize="18sp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/FadeInButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/textView"
        android:layout_marginStart="34dp"
        android:layout_marginTop="63dp"
        android:text="FADE IN"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/FAdeOutButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/FadeInButton"
        android:layout_below="@+id/FadeInButton"
        android:layout_marginTop="28dp"
        android:text="FADE OUT"
        tools:ignore="HardcodedText" />

    <Button
        android:id="@+id/ZoomButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ZOOM IN"
        tools:ignore="HardcodedText"
        android:layout_below="@+id/FAdeOutButton"
        android:layout_alignEnd="@+id/FAdeOutButton"
        android:layout_marginTop="15dp" />

    <Button
        android:id="@+id/FlyButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/ZoomButton"
        android:layout_below="@+id/ZoomButton"
        android:layout_marginTop="40dp"
        android:text="FLY"
        tools:ignore="HardcodedText" />
</RelativeLayout>
